namespace CodeAnalysis {

 enum SyntaxKind {
            NumberToken,
            WhitespaceToken, 
            PlusToken,
            SlashToken,
            StarToken,
            OpenParenthesisToken,
            CloseParenthesisToken,
            MinusToken,
            BadToken,
            EndOfFileToken,
            NumberExpression,
            BinaryExpression,
            ParenthesiszedExpession
    }
}